NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: Postgres URI for running SQL migrations locally (npm run db:apply-003).
# Dashboard → Project Settings → Database → Connection string → URI (do not commit real passwords).
# DATABASE_URL=postgresql://postgres.[ref]:YOUR_PASSWORD@db.[ref].supabase.co:5432/postgres
